<header
  style="
    position: fixed; /* 상단 고정 */
    top: 0;
    left: 0;
    width: 100%; /* 화면 전체 너비 */
    display: flex;
    justify-content: space-between; /* 왼쪽과 오른쪽 정렬 */
    align-items: center;
    background-color: #333;
    color: white;
    padding: 1rem 2rem; /* 상하 패딩 1rem, 좌우 패딩 2rem */
    z-index: 1000; /* 다른 요소 위에 표시 */
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1); /* 헤더 하단 그림자 추가 */
    box-sizing: border-box; /* 패딩 포함하여 크기 계산 */
  "
>
  <h1 style="margin: 0; font-size: 1.5rem">FitConnect</h1>
  <nav>
    <a
      href="/logout"
      style="
        color: white;
        text-decoration: none;
        background-color: #007bff;
        padding: 0.7rem 1.5rem; /* 버튼 패딩 조정 */
        border-radius: 5px;
        font-weight: bold;
        font-size: 1rem;
        display: inline-block;
        margin-left: auto; /* 버튼을 오른쪽으로 밀어냄 */
      "
      >Logout</a
    >
  </nav>
</header>
